// 빌드툴이 그래들이면 콘솔창에 내용 추가됨

// 성능이 중요하면 index 위주, 정확성이 중요하면 FK

// this와 super는 인스턴스 메소드의 변수 접근을 위한 것이며(static 메소드는 접근 불가)
// this(인수)를 다른 생성자 내에 넣어 기본 생성자로 인수를 활용하여 초기화를 진행가능 [car(){this(인수)}]
// static 메소드는 변수에 직접 접근이 가능하지만 인스턴스 메소드는 get변수()를 통해 접근해야함